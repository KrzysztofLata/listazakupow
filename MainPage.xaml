<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.MainPage"
              Style="{StaticResource bckGrn}"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls">
 
        <Grid HorizontalOptions="Center" RowDefinitions="*,*,*,*,*,*" ColumnDefinitions="*,*,*" WidthRequest="300" HeightRequest="700" VerticalOptions="Start">
        <Image Source="biedronka.png" Grid.Row="0" Grid.Column="0"   Margin="10"></Image>
        <Image Source="lidl.png" Grid.Row="0" Grid.Column="1" Margin="10"></Image>
        <Image Source="dino.jpg" Grid.Row="0"  Margin="10" Grid.Column="2"></Image>
        <zxing:CameraBarcodeReaderView
                x:Name="cameraView"
                BarcodesDetected="CameraView_BarcodeDetected"
                IsTorchOn="False"
                IsDetecting="True"
                Grid.Row="1"
            Grid.Column="0"
            Margin="10"
            Grid.ColumnSpan="2"
                      />
        <Label Text="Lista zakupów" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="1" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" FontSize="22"></Label>
        <Entry x:Name="additem" WidthRequest="170" HeightRequest="50" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2"></Entry>
        <Entry x:Name="setquantity" WidthRequest="20" HeightRequest="50" Grid.Column="3" Grid.Row="2"></Entry>
        <Button Style="{StaticResource btnStyle}" Clicked="AddBtn_Clicked" Text="Dodaj" WidthRequest="90" HeightRequest="50" Grid.Column="0" Grid.Row="5" FontSize="18" BorderColor="Black"></Button>
        <CollectionView Style="{StaticResource labStyle}" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="3" Grid.RowSpan="2" x:Name="collectionview" ItemsSource="{Binding Products}" SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,*" Padding="10">
                       
                        <Label Grid.Column="0" Text="{Binding Name}" VerticalOptions="Center" />
                        <Label Grid.Column="1" Text="{Binding Quantity}" VerticalOptions="Center" HorizontalOptions="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Clicked="DelBtn_Clicked" Text="Usuń" WidthRequest="90" HeightRequest="50" FontSize="18" Grid.Column="3" Grid.Row="6" BorderColor="Black"></Button>
       
    </Grid> 
</ContentPage>
